#!/bin/bash
# scan_secrets.sh — stub mínimo (substitui versão legacy)
# A verificação real de secrets é feita em produção pelo scan-truffleHog/ggshield
# em CI. Este stub apenas permite commits locais sem bloquear.
exit 0
